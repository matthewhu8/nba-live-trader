# Project State — 2026-08-10 (Level 2)

**Supersedes `docs/STATE_2026-08-05.md`** for §6.1 only; everything else in that file still
stands. Level 1 was the backtest measurement fixes (`77b0241`). This is Level 2: the exit
simulator's missing feed delay, the last item gating a retrain.

---

## 1. What changed

`models/targets/exit_simulator.py::build_trajectory_targets` anchored every offset at each
possession's `wall_clock_ts`, while `yes_bid` on the same row came from the asof join at
`wall_clock_ts + feed_delay_seconds` (`dataset.py:680`). The labels were **anti-causal, not
merely early**: TP and SL resolved against ticks that preceded the price the position entered
at, so Head B learned to expect fills that were already in the past.

| change | file | effect |
|---|---|---|
| Anchor at `wall_clock_ts + feed_delay_seconds` | `exit_simulator.py` | fixes both `future_*` filters, the checkpoint grid and the time gate from one name |
| `feed_delay_seconds` is **required**, no default | `exit_simulator.py` | omitting it *was* the defect; a new call site must state it |
| Pass the join's delay through | `dataset.py:1003` | entry price and exit window can no longer be anchored at different moments |
| TP clamp moved inside `simulate_exit` | `exit_simulator.py` | post-exit checkpoints freeze at the collectable price, not the overshoot |
| Clamp deleted from the call site | `mmoe_backtest.py` | was duplicated; behaviour unchanged |
| Head B comparability instrumentation | `trainer.py` | see §4 |
| TP clamp to `effective_tp` | `dynamic_exit.py` | same defect 2, third instance; `effective_tp` not `tp` because the streak rule re-posts the limit further out |
| `take_profit_widened` added to `_MAKER_EXIT_REASONS` | `mmoe_backtest.py` | a widened TP still rests; the sweep was charging 4x taker on it |

The horizon is now a **true `horizon_seconds` from entry**. It was `wall_clock_ts + horizon`,
i.e. 100s of real hold at a 20s delay, which also meant stops in the final 20s were labelled
flat `time_gate` exits.

## 2. Verified in both directions

The Level 1 lesson (§3.9 of the 2026-08-05 state: the old exit-window guard was a tautology
that never fired) applied here, so each new test carries its own control at
`feed_delay_seconds=0`, and the production fix was reverted to confirm the tests catch it:

| control | result |
|---|---|
| Anchor reverted to `wall_clock_ts` | **4 tests fail** (anchor, checkpoint grid, true-horizon, TP freeze) |
| TP clamp disabled in `simulate_exit` | **3 tests fail** (both `entry_side` cases + the label-side freeze) |
| `dynamic_exit` clamp + maker reason reverted | **2 tests fail** |
| All restored | **108 pass** (was 102; +6 tests) |

## 3. The backtest baseline did not move

```bash
python -m backtesting.mmoe_backtest --use-traj-for-side --min-abs-traj 0.08 \
  --min-run-length 2 --hold-seconds 240 --threshold 0.15 --traj-aggregator mean
```

| trades | win rate | gross | fees | net |
|---|---|---|---|---|
| 181 | 26.0% | −$6.00 | $318.35 | **−$324.35** |

Reproduced to the digit after the change, as predicted: the backtest already clamped its own
copy of the TP, and Level 2 touched only the label builder — not the features, the cache or
the measurement path. **This stays the yardstick for any retrained model.** Exit-reason mix
unchanged (stop_loss 40.9%, momentum_flip 32.6%, take_profit 24.3%, time_gate 2.2%).

Re-run a second time after `take_profit_widened` joined `_MAKER_EXIT_REASONS`, also unchanged —
`simulate_exit` never emits that reason, so the constant is inert on this path.

**Every `tools/sweep_dynamic_exit.py` result from before 2026-08-10 is unusable**, not merely
biased: gross was over-credited by the unclamped overshoot while fees were over-charged at the
taker rate, so the two errors pushed in opposite directions and the net sign is unknown. Any
widened-TP conclusion drawn from those sweeps needs re-deriving.

## 4. Head B's metrics are not comparable across this change

The full argument is in `skills/model-provenance.md`. The short version, because the
temptation to retrain and check whether `loss_b` improved is the one wrong move available:

`trainer.py::_compute_loss` masks Head B to rows where `mean |traj| > 0.02` — a threshold on
the labels themselves. The anti-causal early TP/SL hits were the high-magnitude rows that
cleared it, so **both the target values and the row population moved**. `loss_b` is now an
average of a different quantity over a different set of rows. Head B can post a *better*
`loss_b` purely by being scored on fewer, easier rows. And since selection is on total val
loss, `loss_b`'s scale change shifts which epoch is kept, so Head A's and Head C's numbers
are individually well-defined but *which checkpoint you have* is not.

Instrumentation added to make the comparison recoverable — `head_b_rows` /
`head_b_rows_total` (mask retention), `rmse_b_zero` (predict-nothing reference, logged as an
`RMSE_B/zero-pred` ratio), `rmse_b_per_checkpoint` (logged beside each saved checkpoint).

**Head C was never affected.** `CLAUDE.md`, `data-integrity.md` and the 2026-08-05 state all
said the simulator built "Head B and Head C" labels. Head C's hazards come from
`kalshi_targets.add_hazard_targets`, indexed over the next N *scoring possessions* rather than
wall clock, and never touch the exit simulator. Only `traj_0..9` moved. All three corrected.

## 5. Next, in order

1. **Score `models/saved/mmoe_delay20.pt` on regenerated labels.** No retrain, inference
   only — features and scaler are untouched. Gives old-model/new-truth against
   new-model/new-truth on shared targets, and quantifies how much of Head B's apparent skill
   was the leakage. Do this *before* retraining; it is the cheapest real measurement here.
2. **Re-extract and retrain.** Expect the mask retention to fall and the exit-reason mix to
   shift toward `time_gate` — both predictions to check, not assumptions.
3. **Re-baseline end-to-end in dollars** against −$324.35, config quoted alongside.
4. **Then** re-derive `min_abs_traj_entry` (`trading.yaml:40`, still flagged contaminated) and
   re-run the event-trigger experiment.

Level 3 (cache re-export: seven dead non-market features, real warehouse joint counts) is
unchanged and still open. No live session since 2026-06-09; nothing deployed depends on this.

## 6. Two data-quality effects to expect, not yet measured

Neither needs code; both are visible in the first re-extract.

- Labels now reach 20s further into absolute time, so some late-game rows will run out of
  ticks and fall back to `time_gate` on a stale price (`_lookup_price_at_or_after`'s fallback).
- The exit-reason distribution should shift generally — fewer near-0s TP/SL hits, since those
  were the anti-causal ones. If it *doesn't* shift, that is worth investigating before
  trusting the re-extract.
